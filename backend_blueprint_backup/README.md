# Backend

سيحتوي هذا المجلد على مشروع `Laravel 11` الخاص بالـ API والخدمات الخلفية.

## الخطة المباشرة

1. إنشاء مشروع Laravel.
2. إعداد `Sanctum`.
3. إعداد `Spatie Permission`.
4. بناء طبقة `Multi-Tenancy` على مستوى المدرسة.
5. إنشاء أول migrations: `roles`, `users`, `schools`.
